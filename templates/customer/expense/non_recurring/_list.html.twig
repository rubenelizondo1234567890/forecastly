{# templates/customer/expense/non_recurring/_list.html.twig #}
<div class="non-recurring-expenses-list">
    <div class="section-header">
        <h2 class="section-title">Your Non-Recurring Expenses</h2>
        <span class="accounts-count">{{ expenses|length }} one-time expenses</span>
    </div>

    <div class="accounts-table-container">
        {% if expenses is not empty %}
            <table class="accounts-table">
                <thead>
                <tr>
                    <th>Expense Source</th>
                    <th>Amount</th>
                    <th>Date</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for expense in expenses %}
                    <tr>
                        <td data-label="Expense Source">
                            <div class="account-type">
                                <div class="account-type-icon expense-icon">
                                    <i class="fas fa-money-bill-wave"></i>
                                </div>
                                <div>
                                    <div>{{ expense.name }}</div>
                                    <small>{{ expense.account.name }}</small><br />
                                    <small>{{ expense.budgetTrackingGroup.name }}</small>
                                </div>
                            </div>
                        </td>
                        <td data-label="Amount" class="negative-balance">
                            ${{ expense.amount|number_format(2) }}
                        </td>
                        <td data-label="Date">{{ expense.dateToApply|date('Y-m-d') }}</td>
                        <td data-label="Actions">
                            <div class="action-buttons">
                                <a href="{{ path('customer_non_recurring_expense_edit', {'id': expense.id}) }}" class="action-btn edit-btn">
                                    <i class="fas fa-edit"></i>
                                </a>
                                <form method="post" action="{{ path('customer_non_recurring_expense_delete', {'id': expense.id}) }}" onsubmit="return confirm('Are you sure you want to delete this non-recurring expense?');" style="display: inline-block;">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expense.id) }}">
                                    <button type="submit" class="action-btn delete-btn">
                                        <i class="fas fa-trash"></i>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        {% else %}
            <div class="empty-state">
                <i class="fas fa-money-bill-wave"></i>
                <p>No non-recurring expenses found.</p>
                <a href="{{ path('customer_non_recurring_expense_index') }}" class="btn btn-primary">Add Your First Non-Recurring Expense</a>
            </div>
        {% endif %}
    </div>
</div>